{{!-- 글쓰기 페이지 작성하기 --}}
<h1>[{{title}}]글 작성</h1>
<div>
    {{!-- (1)글쓰기 폼 --}}
    <form name = "boardForm" method = "post" action = "/write">   {{! 글 작성 form의 name 지정(다른 곳에서 찾을 수 있도록), url이 write인 핸들러 함수 필요(추후 작성)}}
        {{!-- (2) 제목 입력 --}}
        <div>
            <label>제목</label>
            <input type = "text" name = "title" placeholder = "제목" value = ""/>
        </div>
        {{!-- (3) 이름 입력 --}}
        <div>
            <label>이름</label>
            <input type = "text" name = "writer" placeholder = "이름" value = ""/>
        </div>
        {{!-- (4) 비밀번호 입력 --}}
        <div>
            <label>비밀번호</label>
            <input type = "password" name = "password" placeholder = "비밀번호" value = ""/>
        </div>
        {{!-- (5) 본문 입력 --}}
        <div>
            <label>본문을 입력해주세요</label><br/>
            <textarea placeholder="본문" name = "content" cols = "50" rows = "10"></textarea>
            <br/>
        </div>
        {{!-- (6) 버튼 영역 --}}
        <div>
            <button type = "submit">저장</button>
            <button type = "button" onclick = "location.href = '/'"> 취소</button> {{!취소 버튼을 누를 시 리스트 페이지로 돌아가도록 설정}}
        </div>
    </form>
</div>